{
  "name": "esroute-workspaces",
  "scripts": {
    "build": "yarn workspaces foreach -vp run build",
    "test": "jest",
    "start": "yarn workspaces foreach -vpi run start",
    "typecheck": "yarn workspaces foreach -vp run typecheck"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.2.164",
    "@swc/jest": "^0.2.20",
    "@types/jest": "^27.4.1",
    "jest": "^27.5.1",
    "jest-mock": "^27.5.1",
    "regenerator-runtime": "^0.13.9",
    "typescript": "^4.6.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.2.0"
}
